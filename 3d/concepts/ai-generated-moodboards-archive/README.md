These earlier generated images are archived as rough visual moodboards only.

They are not approved geometry references. Some terminal screw counts and finish styles are inaccurate for the simulator, and some options include a used/jobsite look that has been rejected.

Use `../accuracy-locked/` plus `../../data/asset-manifest.json` for active asset approval.
